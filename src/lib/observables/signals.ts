// import { makeSignal } from 'lib/make-signals.lib';
// import { Subject } from 'rxjs';

// export const orderTimeoutSignal = makeSignal<boolean>(new Subject());
// export const orderMutated = new Subject<
//   'declined' | 'accepted' | 'delivered'
// >();
export {}
